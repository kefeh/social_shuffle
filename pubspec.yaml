name: social_shuffle
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+2

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.3
  flutter_svg: ^2.2.3
  uuid: ^4.5.2
  flutter_riverpod: ^3.0.3
  hive: ^2.2.3
  google_generative_ai: ^0.4.7
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/decks/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/my_app_icon.png"
